const Query = require('./queryResolvers');
const Mutation = require('./mutationResolvers');
const Subscription = require('./subscriptionResolvers');

module.exports = {
    Query,
    Mutation,
    Subscription,
};
